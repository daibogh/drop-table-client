import React from 'react'
import { route } from 'src/utils/createRoute'

const {{pascalCase name}} = route(
  ({ children }) => (
    <div>
      {{pascalCase name}}
      {children}
    </div>
  ),
  '{{pascalCase name}}'
)

export default {{pascalCase name}}

